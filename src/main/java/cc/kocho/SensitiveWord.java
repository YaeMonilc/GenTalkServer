package cc.kocho;

public class SensitiveWord {
    public String sensitiveWord;
    public String replaceWord;
    public SensitiveWord(String sensitiveWord, String replaceWord){
        this.sensitiveWord = sensitiveWord;
        this.replaceWord = replaceWord;
    }
}
